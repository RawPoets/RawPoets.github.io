---
hide_body: false
fields:
- name: title
  type: text
  label: Title
  description: Title
  config:
    required: true
- name: date
  type: text
  label: Date
  description: Date
  config:
    required: true
- name: categories
  type: text
  label: Category
  description: 'The post category in one word, for example: Interview'
  config:
    required: true
- name: permalink
  type: text
  label: Permalink
  description: 'Used to make the URL of the post look nicer. Must start and end with
    a "/"... For example: /raw-99/'
  config:
    required: true
